language: node_js
node_js:
  - "7"
stages:
  - name: deploy-prod
    if: branch = master
  - name: deploy-beta
    if: branch = staging
jobs:
  include:
    - stage: deploy-prod
      script: npm run deploy-prod -- --ftp-username=${FTP_USER} --ftp-pass=${FTP_PASS}
    - stage: deploy-beta
      script: npm run deploy-beta -- --ftp-username=${FTP_USER} --ftp-pass=${FTP_PASS}
notifications:
  email:
    on_success: never
    on_failure: change
